{#.}
  <li class="mf-label{?thread}-message{/thread}-link {?selected}selected{?inAll}-in-all{/inAll}{/selected}" data-label-id="{id}">
    <div class="mf-label-name cm-label-link-name icon-tag" style="{style}">
      <span>{name}</span>
    </div>
  </li>
{/.}
