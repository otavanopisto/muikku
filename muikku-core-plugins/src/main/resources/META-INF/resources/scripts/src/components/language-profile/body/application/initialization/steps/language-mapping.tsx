import * as React from "react";
import { useTranslation } from "react-i18next";

const LanguageMapping = () => {
  const { t } = useTranslation("languageProfile");
  return <div>Kielitaidon tasot </div>;
};

export default LanguageMapping;
